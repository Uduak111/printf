#include "main.h"
/**
 * ptx: prints string
 * @c: string to print
 * Return: no. of bytes
 */
int ptx (char *c)
{
	int count = 0;
	if(c)
	{
	for (count = 0; c[count] != '\0'; count++)
		ptcr(&[count]);
	}
	}
	return (count);
}
